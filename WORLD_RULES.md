# Nirai World Rules

> Niraiは機能美を追求する。
> 正しさと安全性を前提に、目的に必要なものだけを最もシンプル・合理的・効率的な構造で実現する。
> 複雑さを価値とせず、削減・統合・明確な責務を優先する。

このRulesはHolo、Resident、Agentを含むNirai上のすべてのAIへ共通して適用し、個別Promptへ複製せず本ファイルを唯一の正本とする。

## 1. 基本原則

### 1.1 シンプル・合理・効率

目的を満たす最小の構造を選ぶ。
追加より削減、複製より統合、例外より共通化を優先する。
理解コストも実装コストとして扱い、初見でも少ない情報で正しく理解できる構造を目指す。

### 1.2 一つの責務、一つの正本

一つの仕組みには明確な責務を持たせる。
同じ状態、設定、判断を複数箇所へ独立して持たない。

### 1.3 設計してから作る

実装前に、目的、必要性、責務、影響範囲、既存構造との関係を整理する。
手を動かしながら設計を決めるのではなく、構造を定めてから実装する。
設計判断に必要な前提が不明な場合のみMasterへ確認する。

### 1.4 足す前に整理する

新しい要求が既存構造と噛み合わない場合、継ぎ足す前に既存構造の整理、削減、統合で解決できないか検討する。
「とりあえず追加する」「念のため経路を増やす」を常態化させない。

## 2. 開発運用

### テスト

テストは目的ではない。
少数の強い恒久テストで、Niraiの重要な契約を広く守る。

恒久テストは原則として以下に限定する。

- **Invariant**：何があっても破ってはいけない不変条件。
- **Critical Flow**：Task開始から完了、Pause / Resume、Restart Recovery等の主要フロー。
- **Boundary**：Hub Store、Capability、Provider等の重要な責務境界。

実装前に、その変更で何を恒久テストとして守るかを決める。
同じ保証を複数のテストで重複して確認せず、実装詳細や一時的な内部構造を固定するテストは作らない。

特殊ケース、一度限りのMigration、障害調査等は**一時検証**として必要時だけ作り、目的達成後に削除する。
一時検証で見つかった問題が今後も成立する重要な契約であれば、専用Regression Testを残すのではなく上位Invariantへ吸収する。

開発中は変更箇所に直接関係する最小限の検証だけを行う。
広い統合テストや全体テストは、統合点や最終確認など必要な区切りに限定し、変更のたびに繰り返さない。

### 文書

設計書には現在の正しい仕様だけを残す。
検討履歴、作業進捗、日付付きメモ、廃止済み案は蓄積せず、履歴はGitへ委ねる。
現在の仕様を理解するために必要な理由は残すが、過去の経緯そのものは残さない。
同じ仕様やルールを複数文書へ複製しない。

## 3. Masterへの報告

Masterへの説明、進捗、完了報告は、平易な日本語で簡潔かつ具体的に伝える。
専門用語が必要な場合は、意味を噛み砕いて説明する。
